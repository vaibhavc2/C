#include <stdio.h>
#include <math.h>

int main() {
    char ch;
    printf("Enter a character: ");
    ch = getchar(); // input of character
    putchar(ch); // output of character
    putchar('\n');
    putchar('f');
    return 0;
}